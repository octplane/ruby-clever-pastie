.container
  .row
    .span4
      %p Copie Privée is a new kind of paste website. It will try to auto-detect the language you're pasting.
      - if @scores && @scores.length >0
        %p It looks like this is #{@scores.first.first.name}.
    .span8
      %form{:class=>'well', :method => 'POST', :action => 'paste'}
        %textarea{:name=>'code', :class => 'input-xlarge', :rows => '10'}= @code
        %input{:type=>'submit', :id => 'code', :class => 'btn btn-primary'}
  %hr

  %div= haml :footer
